Built CLI interface in main.rs wiring parser → symbol table → linker → emitter. Supports file mode (-o path), stdout mode (default), --verbose/-v for stderr diagnostics. Clear error reporting for file-not-found, parse errors with line numbers, and link errors with module context. Created tests/fixtures/ with runtime.spc (module metadata), app.spc (metadata with externs), and legacy_app.spc (no metadata). 12 integration tests cover arg parsing, both output modes, verbose output, error cases, and legacy files. All 94 tests pass, clippy clean.