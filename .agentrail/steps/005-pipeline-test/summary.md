Added end-to-end pipeline tests using the real pr24p phase 0 runtime with module metadata. Created e2e_runtime.spc (all 3 runtime procs with .module/.export) and e2e_app.spc (calls write_int, write_bool, write_ln). 6 pipeline tests verify: correct linking with real runtime, output reparsability, verbose diagnostics, file/stdout parity, input-order independence, and runtime-only error. Added scripts/pipeline.sh demonstrating the full pl24r→pasm→pv24a flow. Updated CLAUDE.md with pipeline docs. pasm/pv24a not installed on host so downstream steps are documented but not exercised. All 100 tests pass, clippy clean.