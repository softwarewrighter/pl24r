End-to-end integration test with the real Pascal toolchain. (1) Copy or reference the phase0 runtime from ~/github/softwarewrighter/pr24p/src/runtime.spc, (2) use a simple Pascal program compiled by p24p to .spc (or hand-write a small app.spc that calls _p24p_write_int and _p24p_write_ln), (3) run pl24r to combine runtime.spc + app.spc into combined.spc, (4) verify combined.spc assembles cleanly with pasm, (5) verify the resulting .p24 runs correctly on the emulator producing expected output. Fix any issues found in the pipeline. Document the full build flow in a project README or CLAUDE.md. If pasm or p24p are not available on the host, create a shell script showing the intended pipeline and test with manual verification. Ensure all cargo tests pass, clippy is clean, and the project is ready for use.