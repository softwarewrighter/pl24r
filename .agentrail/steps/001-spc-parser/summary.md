Created pl24r Rust project scaffold with complete .spc parser. src/lib.rs exposes parser module, src/parser.rs implements full .spc text parsing into structured AST (Module with name/exports/externs/items, Item variants for Proc/Global/Data/Const/Comment, Statement variants for Label/Instruction/Comment/Blank). Handles all existing directives (.proc/.end/.global/.data/.const), labels, instructions with operands and inline comments, plus new module metadata (.module/.endmodule/.export/.extern). Export-all fallback for files without metadata. 17 unit tests all passing, clippy clean.